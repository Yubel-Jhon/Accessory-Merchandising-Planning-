# 商品企划端 · 产品设计 v0.2（最小核心能力版）

> 参考 MuseGate（我上家公司）+ 绘蛙 + LOOK AI 的能力排布与界面，砍到最小核心，只做「企划端」。钟于泊 · 2026-09-01

---

## 0. 我参考了什么（三个产品的排布方式）

### MuseGate（杭州霖润智能，创始人甲第·前阿里天猫3D试衣负责人）
**能力按「价值链」排成一条链路：**

```
灵感/改款 → 模特图/视频 → 面料质感 → 社媒种草 → 营销/运营
(设计)      (表现)         (表现)     (营销)      (营销)
```

- 灵感改款：输入关键词（如「复古菱格+莫兰迪色系」）→ 几秒出几十种方案
- 模特/视频：虚拟模特捏脸（年龄/人种）+ 一键换景（T台/街拍）
- 面料质感：还原纱线蓬松感、针织纹理
- 营销：社媒素材、智能混剪视频、广告投放、全链路运营
- 定价：约 40 元/100 张 vs 实拍几百元/张；服务波司登/戎美/毛衫派

### 绘蛙（阿里系，ihuiwa.com）—— 界面交互的范本
**不是自由画布，是「锚点图 + 下拉选项」引导流：**

```
上传平铺图 → 选模特(50+库) → 选场景 → 选姿势 → 生成上身图
```

这正好验证了我们的「下拉向导 + 锚点图」方向——用户从不写 prompt，全程点选。

### LOOK AI（look-ai.cn）—— 设计端
线稿实时生成、款式图、材质转换、图案提取。

---

## 1. 关键洞察：MuseGate 的链路 = 「设计 + 表现 + 营销」

**我们的企划端，只取前两段「设计 + 表现」，砍掉「营销」。**

| MuseGate 全链路 | 我们企划端 |
|---|---|
| 灵感/改款 | ✅ 锚点图（起盘） |
| 模特/场景 | ✅ 图→图派生（白底/商拍/氛围/模特） |
| 面料质感 | ✅ 面料图 |
| ~~社媒种草~~ | ❌ 砍 |
| ~~视频混剪/广告/运营~~ | ❌ 砍（营销端不是企划端） |

**企划端最小核心 = 3 件事：**
1. **一张锚点图**（上传或生成）
2. **图→图派生**（锚点派生到七类图，用转换矩阵约束）
3. **累积成企划**（多图 + 编排 + 导出 PPT）—— 这是 MuseGate/绘蛙都没有的差异化

---

## 2. 能力排布：按「图类型轨道」排（不是按营销链路）

MuseGate 按营销链路排，我们按「图库」排成一条轨道：

```
锚点图 ──→ 白底 ──→ 商拍 ──→ 氛围 ──→ 细节 ──→ 面料 ──→ 说明
                              └──→ 模特 ──┘
```

- 每个图类型 = 轨道上一个**节点**，转换矩阵决定「哪些节点能点」。
- 用户从锚点出发，逐个点亮节点，点亮的图自动累积进企划。

---

## 3. 界面布局（参考绘蛙的引导式，左→右）

```
┌─────────────────────────────────────────────────────────────┐
│  商品企划生成器                          [导出企划 → PPT]      │
├─────────────────────────────────────────────────────────────┤
│ ① 锚点图（左）     │ ② 图类型轨道（顶）                        │
│ ┌───────────┐     │  白底 → 商拍 → 氛围 → 细节 → 面料 → 说明  │
│ │  [上传]    │     │  ○ 当前   ● 已生成   ○ 可点   ✕ 不可点     │
│ │  [识别:白底]│     │      (转换矩阵自动亮/灰)                  │
│ └───────────┘     ├──────────────────────────────────────────┤
│                   │ ③ 下拉选项（一条条，不写 prompt）          │
│                   │  目标图: [商拍 ▾]  品类: [餐厨→水杯 ▾]      │
│                   │  材质: [不锈钢▾]  颜色: [莫兰迪粉▾]         │
│                   │  场景: [影棚▾]    零售商: [山姆▾]  数量:[3▾] │
│                   │  [生成这张图]                              │
├─────────────────────────────────────────────────────────────┤
│ ④ 结果区（右/下）：3 张变体横向排列 → 点选 1 张纳入企划         │
│  [图1] [图2] [图3]      （风格统一，同一 seed）                │
└─────────────────────────────────────────────────────────────┘
```

**四个区块的职责（对应绘蛙的「上传→选→生成」）：**
1. **锚点图**：上传即识别类型（白底/商拍/氛围…），可手动改。相当于绘蛙的「上传平铺图」。
2. **图类型轨道**：七类图节点，转换矩阵自动亮/灰，显示哪些已生成、哪些可点。
3. **下拉选项**：目标图类型 / 品类 / 材质 / 颜色 / 场景 / 零售商 / 数量。全程点选，不写 prompt。
4. **结果区**：多张变体横向选，选中的纳入企划。

---

## 4. 一个完整案例（白底 → 商拍 → 氛围 → 企划）

```
1. 上传一张白底保温杯图 → 系统识别「白底图」
2. 轨道亮起：商拍/氛围/细节/面料/说明/模特 都可点
3. 下拉：目标=商拍，品类=餐厨→水杯保温杯，材质=不锈钢，颜色=莫兰迪粉，零售商=山姆，数量=3
4. 生成 3 张商拍变体 → 选中 1 张
5. 再点「氛围图」节点 → 下拉：场景=现代厨房，人群=年轻家庭 → 生成 → 选中
6. 再点「细节图」→ 卖点=真空杯口螺纹 → 生成 → 选中
7. 点「导出企划」→ 自动按 氛围→商拍→白底→细节 编排成 1 页推介 PPT
```

---

## 5. 为什么这是「最小」——砍掉的清单

| 砍掉（MuseGate/绘蛙有，但企划端不需要） | 原因 |
|---|---|
| 虚拟模特捏脸（年龄/人种） | 营销端功能，企划端只要「上身效果」一张图 |
| 一键换景（T台/街拍） | 场景下拉里给几个预设就够，不做无限换景 |
| 智能混剪视频 | 营销端，砍 |
| 广告投放 / 社媒运营 / 全链路 | 完全砍 |
| 种草文案生成 | 企划端只出「选品逻辑」一段话，不写小红书文案 |
| 线稿实时设计（LOOK） | 那是「设计端」，我们是「企划端」，起盘用上传图+文字就行 |

**留下的是唯一不可替代的一条：锚点图 → 图→图派生 → 编排成企划。** 别人有生图，但没有「企划」这个产物。

---

## 6. 与 v0.1 的衔接（数据/接口不变）

- 转换矩阵、七类图 prompt 模板、品类库、编排规则 → 见 `商品企划Agent-产品设计.md`。
- 本 v0.2 只重新定义了**「端」的交互与能力排布**（前端层），后端数据模型不变。

---

## 7. 下一步

1. 确认这个「轨道式 + 下拉向导」的企划端对不对（比 v0.1 的纯下拉多了「图类型轨道」这个可视化的东西）。
2. MVP 前端形态：**Skill + HTML 结果页**（先跑通）→ 后面升级成上面这个真前端。
3. 定完我先搭 `trend-to-product`，跑通「白底→商拍」这一条转换 + 3 张变体 + 编排。

---

### 参考来源
- MuseGate 能力/公司：[Z Potentials 甲第访谈（腾讯新闻）](https://news.qq.com/rain/a/20260227A0313Z00)、[搜狐转载](https://www.sohu.com/a/990524732_122063396)、[杭州霖润智能百度百科](https://baike.baidu.com/item/杭州霖润智能科技有限公司)
- MuseGate 应用（洪合毛衫/广东时装周）：[中国纺织报](https://www.ctn1986.com/index.php?c=content&a=show&id=118918)、[浙江在线](https://zjnews.zjol.com.cn/zjnews/202412/t20241220_30721445.shtml)
- 绘蛙：[绘蛙评测·AI星球](https://www.aixq.cc/37734.html)、[绘蛙官网介绍](https://jingzhunlink.com/sites/2932.html)
- LOOK AI：[look-ai.cn](https://look-ai.cn/)
