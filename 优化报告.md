# 服装企划agent 优化报告

> 依据三个输入源：① 利丰 JD 功能拆解 ② 9/5 代码验证结果 ③ COLE HAAN FW26 真实企划 deck（29页）+ 15 页重构大纲。
> 日期：2026-09-05

---

## 一句话结论

agent 目前是「**单款图库工具**」：选一个 SKU → 出 7 类图 → 导出 1 页拼图 PPT。
COLE HAAN 真实 deck 证明，产品企划岗的交付物是「**多 SKU 企划盘 deck**」：结构总表 + 逐款页（参数条/双价格/设计方向）+ 矩阵页 + 开发日历。
**优化核心：把导出物从 1 页拼图升级为 15 页企划 deck，数据模型从单 SKU 升级为企划盘。** 生图链路（qwen i2i / 三轴演变）不用动——那部分已经对齐 JD 核心。

---

## 1. 现状定位（已验证）

| 层 | 现状 | 评价 |
|---|---|---|
| 生图链路 | qwen-image i2i、7 类图、三轴演变（VARIATION_REF_LOCK 锁 DNA）、qwen-vl 识别锚点 | ✅ 对齐 JD 功能 2，不动 |
| 数据模型 | 3 方向 × 4 SKU；sku 字段：material/craft/fabric_type/persona/colors/scenes | ⚠️ 缺企划字段（见 §3-P0） |
| 企划流程 | 单 SKU：选款 → 出图 → 变体 → 导出 | ❌ 无「盘子」概念 |
| 导出 | 单页 PPT（hero + 3 缩略图 + 选品逻辑卡）+ 单页 HTML；价格硬编码「¥299–499」 | ❌ 与真实 deck 差一个量级 |

## 2. 目标形态：agent 自动产出重构版 15 页 deck

| 页 | 内容 | 数据来源 |
|---|---|---|
| P01 封面 | 方向 × 季节 + 「AI 企划 · 45 分钟」 | plan 元信息 |
| P02 企划方法 | 4 步流程 + 工具/耗时 | 静态模板 + 耗时统计 |
| P03 人群画像 | 3 类人群 | **新增字段** direction.personas |
| P04 产品结构总表 | 品类 × SPU × SKU × TTL | **企划盘数据**（plan.skus 聚合） |
| P05-P08 逐款页 | 图 + **参数条（成分/规格/细度）+ 双价格 + 演变轴标注** | **新增字段** + 演变记录 |
| P09-P11 矩阵页 | 帽子/手套/袜子/皮带 拼版 + 价格 | 企划盘数据 |
| P12 AI 出图体系 | 单 SKU × 6 类图矩阵 | 现有导出逻辑挪过来 |
| P13 开发日历 | 8 节点时间线 + AI 压缩标注 | 静态模板 |
| P14 尾页 | 工具链 + **真实耗时拆解** | 静态模板 + 耗时统计 |

## 3. 改造清单（按优先级）

### P0-A 数据字段补齐（directions.json）—— COLE HAAN deck 反推出的字段标准

真实 deck 每款页必带：成分百分比 / 规格尺寸 / MSRP+WHOLESALE。当前 sku 对象全缺：

```
sku 新增：
  composition   "100%山羊绒"          # 成分（参数条）
  spec          "180×30CM（含穗）"     # 规格尺寸
  fineness      "16.5um"              # 细度（面料类）
  price         { "msrp": 299, "wholesale": 168, "currency": "¥" }
  attributes    { "场景": "...", "功能": "...", "季节": "...", "风格": "..." }  # 袜子式标注，可空
  design_directions [ { "axis": "detail", "desc": "CH 字母提花" }, ... ]  # 设计方向=演变轴语言

direction 新增：
  personas      [ 3 类人群画像文案 ]
  calendar      [ 8 节点开发日历 ]（或放全局常量）
```

其中 `design_directions` 是关键洞见：**原 deck 的「设计方向」就是演变轴，只是没用 AI 岗的语言讲**。结构化后，逐款页的「设计方向」和演变功能的 change_desc 共用一套语言。

### P0-B 导出重写（exporter.py）：1 页 → 15 页 deck 生成器

- 7 种页面模板函数：封面 / 方法 / 人群 / 总表 / 逐款 / 矩阵 / 体系 / 日历 / 尾页（方法/日历/尾页为静态模板，一次成本）
- 逐款页布局（对齐 COLE HAAN P4）：左图组（2-3 张）+ 右参数条（成分/规格/细度/双价格）+ 底部设计方向（演变轴 + before/after 缩略对比，有演变记录时）
- HTML（多页滚动）和 PPTX（多 slide）同步重写
- 价格从硬编码改为读 sku.price

### P1-A 企划盘流程（单 SKU → 多 SKU）

- plan 结构：`plan.skus[] = { sku, selected{图类型:路径}, variation{before/after/axis/change} }`
- server `/api/export` 接收企划盘；P04 总表 / P09-P11 矩阵页由 plan.skus 聚合生成
- 前端：方向 → SKU 树列表 → 逐个 SKU 出图/演变 → 「纳入企划盘」→ 导出全 deck
- 这是最重的改动（前端状态机重构），放第二期

### P1-B 耗时统计（45 分钟叙事的真实证据）

- JOBS 记录每次生成开始/结束时间，plan 汇总累计耗时
- 尾页「耗时拆解：锚点拆解 X 分 · 出图 X 分 · 组装 X 分」用**真实数据**——面试时这是 agent 真跑出来的数字，不是文案

### P2（按需）

- **视频样稿页**：Seedance 接 `/api/video`，deck 加可选页（JD 明确要视频；COLE HAAN deck 没有，所以放 P2 而非 P0）
- **bug 修复**：① qwen_generate 落盘前校验文件大小，杜绝 0 字节图（9/4 实际发生过）② 参考图库「建而不用」——建议砍掉前端入口、保留落盘，减少演示解释负担 ③ JOBS 内存态重启即丢——演示场景低优先

## 4. 明确不做（范围控制）

| JD 功能 | 处理 | 理由 |
|---|---|---|
| 趋势识别（销售数据接入） | 不做，用「锚点款拆解页」话术替代 | COLE HAAN 真实 deck 也没有数据页，入口是人群画像+产品结构 |
| 协同企划 / 采购系统 | 不做 | 岗位职责非工具功能；deck 的「成分/规格/双价格」字段就是采购对接的语言 |

## 5. 分期与工作量

| 期 | 内容 | 工作量 | 效果 |
|---|---|---|---|
| 一期 | P0-A 字段 + P0-B 导出重写（单 SKU 下先出 ~10 页 deck） | 半天 | 导出物直接变身，demo 效果最大 |
| 二期 | P1-A 企划盘多 SKU（server+前端）+ 总表/矩阵页补全 | 半天～1 天 | 流程对齐「产品企划师」工作方式 |
| 三期 | P1-B 耗时统计 + P2 各项 | 各 1-2 小时 | 真实数据 + 修复瑕疵 |

**演示故事线（优化后）**：载入方向 → 选 SKU 出图/演变（30 秒/张）→ 逐个纳入企划盘 → 导出 15 页企划 deck → 尾页展示真实耗时拆解。完整跑一遍就是「45 分钟企划」的现场证明。
